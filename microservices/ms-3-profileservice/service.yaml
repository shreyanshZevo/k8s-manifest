apiVersion: v1
kind: Service
metadata:
  name: dev-profileservice-service
  namespace: argocd
spec:
  selector:
    app: dev-profileservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 31248
  type: NodePort
